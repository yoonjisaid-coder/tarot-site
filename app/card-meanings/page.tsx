import {cards} from '@/lib/tarot';import {Header,Footer} from '@/components/site-shell';
export const metadata={title:'All 78 Tarot Card Meanings — Untold',description:'Explore upright and reversed meanings for the full tarot deck, with a focus on love and relationships.'};
export default function Meanings(){return <><Header/><main className="library"><span className="eyebrow">Get to know your deck</span><h1>The language of the cards.</h1><p className="library-intro">78 different perspectives. Explore each card’s upright and reversed themes through the lens of love, connection and self-respect.</p>{['Major Arcana','Wands','Cups','Swords','Pentacles'].map(suit=><section key={suit}><h2 style={{fontSize:29,margin:'32px 0 22px'}}>{suit}</h2><div className="card-grid">{cards.filter(c=>c.suit===suit).map(c=><a className="learn-card" href={`/card-meanings/${c.slug}`} key={c.id}><img src={c.image} alt={c.name} loading="lazy" width="160" height="270"/><h2>{c.name}</h2><p>{c.keyword}</p></a>)}</div></section>)}</main><Footer/></>}
