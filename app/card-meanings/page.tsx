import {koCards} from '@/lib/tarot-ko';
import {cards} from '@/lib/tarot';import {Header,Footer} from '@/components/site-shell';
export const metadata={title:'All 78 Tarot Card Meanings — Untold',description:'Explore upright and reversed meanings for the full tarot deck, with a focus on love and relationships.'};
export default async function Meanings({searchParams}:{searchParams:Promise<{lang?:string}>}){const ko=(await searchParams).lang==='ko';const t=(en:string,kr:string)=>ko?kr:en;return <><Header ko={ko}/><main className="library" lang={ko?"ko":"en"}><span className="eyebrow">{t('Get to know your deck','내가 고른 카드 알아보기')}</span><h1>{t('The language of the cards.','카드가 전하는 이야기.')}</h1><p className="library-intro">{t('78 different perspectives. Explore each card’s upright and reversed themes through the lens of love, connection and self-respect.','78장의 서로 다른 관점. 연애와 관계, 나를 존중하는 마음을 중심으로 각 카드의 정방향·역방향 의미를 알아보세요.')}</p>{['Major Arcana','Wands','Cups','Swords','Pentacles'].map(suit=><section key={suit}><h2 style={{fontSize:29,margin:'32px 0 22px'}}>{ko?({"Major Arcana":"메이저 아르카나",Wands:"완드",Cups:"컵",Swords:"소드",Pentacles:"펜타클"}[suit]):suit}</h2><div className="card-grid">{(ko?koCards:cards).filter(c=>c.suit===suit).map(c=><a className="learn-card" href={`/card-meanings/${c.slug}${ko?"?lang=ko":""}`} key={c.id}><img src={c.image} alt={c.name} loading="lazy" width="160" height="270"/><h2>{c.name}</h2><p>{c.keyword}</p></a>)}</div></section>)}</main><Footer ko={ko}/></>}
