import {cards} from '@/lib/tarot';import {notFound} from 'next/navigation';import {Header,Footer} from '@/components/site-shell';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=cards.find(x=>x.slug===slug);return{title:c?`${c.name}: Love, Upright & Reversed Meaning — Untold`:'Card not found'}}
export default async function CardPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=cards.find(x=>x.slug===slug);if(!c)notFound();return <><Header/><main className="article"><a href="/card-meanings">← All 78 cards</a><p className="eyebrow">{c.suit} · {c.keyword}</p><h1>{c.name}</h1><img className="detail-art" src={c.image} alt={`${c.name}, original Waite–Smith card`}/><h2>Upright in love</h2><p>{c.upright}</p><h2>Reversed in love</h2><p>{c.reversed}</p><h2>A question to sit with</h2><p>Where does the theme of {c.keyword.toLowerCase()} show up in your actual experience? Name one behavior you have observed, then one assumption you may be making.</p><h2>Reading this card in context</h2><p>A card’s position changes the question it asks. As a feeling, it can prompt reflection on emotional availability. As an obstacle, it invites you to notice a pattern that needs attention. As a next step, it should lead back to a choice you can make yourself.</p><p>Reversed cards are not automatically negative. They can suggest an inward focus, a blocked theme, an excess or a need to reconsider the upright meaning. These are our editorial interpretations, not claims about another person’s private thoughts.</p><a href="/">Try a love reading →</a><p style={{fontSize:13,clear:'both'}}>Artwork: Pamela Colman Smith, original Waite–Smith deck. See <a href="/about">artwork credits</a>.</p></main><Footer/></>}
